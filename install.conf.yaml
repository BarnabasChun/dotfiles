- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.config/starship.toml: starship.toml

- shell:
  - 
    command: ./setup_homebrew.zsh
    stdout: true
    sterr: true